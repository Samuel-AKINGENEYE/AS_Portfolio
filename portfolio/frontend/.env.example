# API base URL — used by axios in src/services/api.js
# In development the Vite proxy forwards /api → localhost:5000, so this is optional locally.
# Set it for production (e.g. your Render backend URL).
VITE_API_URL=https://your-backend.onrender.com/api
