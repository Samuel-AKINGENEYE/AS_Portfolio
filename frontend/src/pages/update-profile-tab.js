// This is the updated ProfileTab section - replace the existing ProfileTab in AdminDashboard.jsx
// Look for "function ProfileTab() {" and replace everything until the closing "}" with the code below
